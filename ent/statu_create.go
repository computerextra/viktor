// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"viktor/ent/statu"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// StatuCreate is the builder for creating a Statu entity.
type StatuCreate struct {
	config
	mutation *StatuMutation
	hooks    []Hook
}

// SetStatus sets the "status" field.
func (sc *StatuCreate) SetStatus(s string) *StatuCreate {
	sc.mutation.SetStatus(s)
	return sc
}

// SetSince sets the "since" field.
func (sc *StatuCreate) SetSince(s string) *StatuCreate {
	sc.mutation.SetSince(s)
	return sc
}

// SetID sets the "id" field.
func (sc *StatuCreate) SetID(i int32) *StatuCreate {
	sc.mutation.SetID(i)
	return sc
}

// Mutation returns the StatuMutation object of the builder.
func (sc *StatuCreate) Mutation() *StatuMutation {
	return sc.mutation
}

// Save creates the Statu in the database.
func (sc *StatuCreate) Save(ctx context.Context) (*Statu, error) {
	return withHooks(ctx, sc.sqlSave, sc.mutation, sc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (sc *StatuCreate) SaveX(ctx context.Context) *Statu {
	v, err := sc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sc *StatuCreate) Exec(ctx context.Context) error {
	_, err := sc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sc *StatuCreate) ExecX(ctx context.Context) {
	if err := sc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (sc *StatuCreate) check() error {
	if _, ok := sc.mutation.Status(); !ok {
		return &ValidationError{Name: "status", err: errors.New(`ent: missing required field "Statu.status"`)}
	}
	if _, ok := sc.mutation.Since(); !ok {
		return &ValidationError{Name: "since", err: errors.New(`ent: missing required field "Statu.since"`)}
	}
	return nil
}

func (sc *StatuCreate) sqlSave(ctx context.Context) (*Statu, error) {
	if err := sc.check(); err != nil {
		return nil, err
	}
	_node, _spec := sc.createSpec()
	if err := sqlgraph.CreateNode(ctx, sc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int32(id)
	}
	sc.mutation.id = &_node.ID
	sc.mutation.done = true
	return _node, nil
}

func (sc *StatuCreate) createSpec() (*Statu, *sqlgraph.CreateSpec) {
	var (
		_node = &Statu{config: sc.config}
		_spec = sqlgraph.NewCreateSpec(statu.Table, sqlgraph.NewFieldSpec(statu.FieldID, field.TypeInt32))
	)
	if id, ok := sc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := sc.mutation.Status(); ok {
		_spec.SetField(statu.FieldStatus, field.TypeString, value)
		_node.Status = value
	}
	if value, ok := sc.mutation.Since(); ok {
		_spec.SetField(statu.FieldSince, field.TypeString, value)
		_node.Since = value
	}
	return _node, _spec
}

// StatuCreateBulk is the builder for creating many Statu entities in bulk.
type StatuCreateBulk struct {
	config
	err      error
	builders []*StatuCreate
}

// Save creates the Statu entities in the database.
func (scb *StatuCreateBulk) Save(ctx context.Context) ([]*Statu, error) {
	if scb.err != nil {
		return nil, scb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(scb.builders))
	nodes := make([]*Statu, len(scb.builders))
	mutators := make([]Mutator, len(scb.builders))
	for i := range scb.builders {
		func(i int, root context.Context) {
			builder := scb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*StatuMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, scb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, scb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int32(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, scb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (scb *StatuCreateBulk) SaveX(ctx context.Context) []*Statu {
	v, err := scb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (scb *StatuCreateBulk) Exec(ctx context.Context) error {
	_, err := scb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (scb *StatuCreateBulk) ExecX(ctx context.Context) {
	if err := scb.Exec(ctx); err != nil {
		panic(err)
	}
}
